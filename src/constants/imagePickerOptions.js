export const imageLibraryOption = {
  mediaType: 'photo',
  // includeBase64: true,
  // includeExtra: true,
  selectionLimit: 1,
};

export const cameraOption = {
  mediaType: 'photo',
  // includeBase64: true,
  // includeExtra: true,
  savedToPhotos: true,
};
