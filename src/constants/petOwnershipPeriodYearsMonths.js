export const yearOptions = Array.from({length: 30}, (v, k) => ({
  label: `${k + 1}`,
  value: `${k + 1}`,
}));

export const monthOptions = Array.from({length: 11}, (v, k) => ({
  label: `${k + 1}`,
  value: `${k + 1}`,
}));
